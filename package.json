{
  "name": "papercuts",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Tiny tools for tiny internet papercuts.",
  "scripts": {
    "build": "node build.mjs",
    "test": "for s in csv xray json names cron cors csp cache gitignore; do node test/test-$s.mjs || exit 1; done",
    "dev": "npx --yes serve --listen 4399 ."
  }
}
